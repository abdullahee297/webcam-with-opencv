# webcam-with-opencv

This is a simple code that show case how you can open your webcam and if there is no web cam it show a message for not detecting the camera.
Also if you want to exit the window you can simply press q to exit
